



(function(window, undefined) {
	
var 

Class = UI.Class,
UICore = Class.Core,

UIControl = Class({
	name: "UIControl",
	parent: UICore,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIButton = Class({
	name: "UIButton",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UICheckBox = Class({
	name: "UICheckBox",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIRadio = Class({
	name: "UIRadio",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UISwitch = Class({
	name: "UISwitch",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UISelectBox = Class({
	name: "UISelectBox",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UITab = Class({
	name: "UITab",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UISegment = Class({
	name: "UISegment",
	parent: UIControl,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UISlider = Class({
	name: "UISlider",
	parent: UICore,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UITwinSlider = Class({
	name: "UITwinSlider",
	parent: UICore,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
});

window.UIButton = UIButton;
window.UICheckBox = UICheckBox;
window.UIRadio = UIRadio;
window.UISwitch = UISwitch;
window.UISelectBox = UISelectBox;
window.UITab = UITab;
window.UISegment = UISegment;
window.UISlider = UISlider;
window.UITwinSlider = UITwinSlider;
	
})(window);