

(function(window, undefined) {

var 
Class = UI.Class,
UICore = Class.UICore,

UIGraph = Class({
	name: "UIGraph",
	parent: UICore,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGraphLine = Class({
	name: "UIGraphLine",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGraphBar = Class({
	name: "UIGraphBar",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGraphPie = Class({
	name: "UIGraphPie",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGrapheRadar = Class({
	name: "UIGrapheRadar",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGraphDoughnut = Class({
	name: "UIGraphDoughnut",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
}),

UIGraphPolarArea = Class({
	name: "UIGraphPolarArea",
	parent: UIGraph,
	constructor: function( element ) {
		
		return {
			init: function() {
				var self = this._super().init();
				if ( self ) {
					
				};
				return this
			}
		}
	}
});

window.UIGraphLine = UIGraphLine;
window.UIGraphBar = UIGraphBar;
window.UIGraphPie = UIGraphPie;
window.UIGrapheRadar = UIGrapheRadar;
window.UIGraphDoughnut = UIGraphDoughnut;
window.UIGraphPolarArea = UIGraphPolarArea;


})(window);